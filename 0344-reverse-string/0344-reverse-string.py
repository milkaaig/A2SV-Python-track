class Solution:
    def reverseString(self, s: List[str]) -> None:
        """
        Do not return anything, modify s in-place instead.
        """
        size = len(s)
        output = s.reverse()
        # print(output)
        # for  i in range(size):
        #     s[i] = output[i]
        print (s)